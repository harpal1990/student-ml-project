apiVersion: v1
kind: Service

metadata:
  name: student-ml-service

spec:

  selector:
    app: student-ml-api

  type: NodePort

  ports:

  - protocol: TCP
    port: 8000
    targetPort: 8000
    nodePort: 30080